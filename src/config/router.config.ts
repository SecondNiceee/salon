class Router {
  profile = "/profile"
  home = "/"
  checkout = "/checkout"
  orders = "/orders"
  favorited = "/favorites"
  mobileLogin = "/login"
  mobileCatalog = "/catalog"
  product = "/product"
}
export const routerConfig = new Router()
