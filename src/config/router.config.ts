class Router {
  profile = "/profile"
  home = "/"
  checkout = "/checkout"
  orders = "/orders"
  favorited = "/favorites"
  mobileLogin = "/login"
  mobileCart = "/mobile-cart"
  mobileCatalog = "/catalog"
  product = "/product"
}
export const routerConfig = new Router()
